@model IEnumerable<StoreApplication.Data.Entities.Customers>

@{
    ViewData["Title"] = "Search Customer";
}

<h1>Search Customer</h1>
@if (Model.Count() == 0)
{
    <br />
    <br />
    <h3>No Such Record Present.</h3>
    <h5>Please Try And Search For A Different Name</h5>
    <br />
    <br />
    <p><a asp-area="" asp-controller="Customer" asp-action="SearchIndex">Go Back</a></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @*@Html.DisplayNameFor(model => model.CustomerId)*@
                    Customer ID
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.FirstName)*@
                    First Name
                </th>
                <th>
                    @*@Html.DisplayNameFor(model => model.LastName)*@
                    Last Name
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CustomerId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        </tbody>
    </table>
}